name: 'Greetings'
description: 'Greet a person and record the time.'
inputs:
  who-to-greet:
    description: 'Who to greet'
    required: true
    default: 'Person'
outputs:
  time:
    description: 'Time of greeting'
runs:
  using: 'node20'
  main: 'dist/index.js'